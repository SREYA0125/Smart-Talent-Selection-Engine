// Shared constants used across the app. Empty for now — filled in as later
// modules need shared values (e.g. ALLOWED_FILE_TYPES for the Resumes
// module, ROLES for role-based UI). Centralized here instead of scattered
// magic strings/numbers across components.
export const APP_NAME = "Smart Talent Selection Engine";
